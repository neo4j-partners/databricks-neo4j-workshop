= Lab 5 Instructions: Aura Agents

Follow these steps to build an agent using Neo4j Aura's "Create with AI" workflow.

TIP: For concepts and background, see xref:lab5.adoc[Lab 5: Concepts and Reference].

**Duration:** ~30 minutes

== Prerequisites

* Completed **Lab 1** (Neo4j Aura setup)
* Completed **Lab 2** (Databricks ETL)
* Completed **Lab 3** (Semantic Search -- embeddings and vector index)

The knowledge graph you built in Labs 2 and 3 contains the aircraft topology, operational data, and maintenance manual embeddings that the agent tools will use.

== Step 1: Navigate to Agents

1. Go to https://console.neo4j.io[console.neo4j.io]
2. Select **Agents** in the left-hand menu
3. Click **Create with AI**

image::lab5-create-agent.png[Create Agent]

== Step 2: Configure AI Settings

image::lab5-create-ai-setting.png[Create with AI Settings]

Configure the following:

* **Instance:** Select your Neo4j instance (should show **RUNNING** status)
* Check **This instance contains vector embeddings**
* **Embedding provider:** `OpenAI`
* **Embedding model:** `text-embedding-3-small`

**Prompt:**
----
You are an expert aircraft maintenance and operations analyst.
You help users understand:
- Aircraft systems, components, and their hierarchical relationships
- Maintenance events, fault patterns, and corrective actions
- Flight operations, routes, and delay causes
- Sensor configurations and monitoring coverage
- Maintenance manual procedures and troubleshooting steps

Always provide specific examples from the knowledge graph when answering questions.
Ground your responses in the actual aircraft data and maintenance documentation.
----

Click **Create**.

== Step 3: Review Agent Configuration

image::lab5-agent-overview.png[Agent Overview]

Review the generated settings:

* **Name:** The AI suggests a name based on your prompt
* **Description:** An auto-generated summary of the agent's capabilities
* **Prompt instruction:** A refined version of your original prompt
* **Access:** Leave as **Internal** (free) for now -- you can switch to External later if you want API or MCP access

Click **Create agent** when satisfied.

== Step 4: Review the Generated Tools

image::lab5-agent-tools.png[Agent Tools]

The "Create with AI" workflow generates tools based on your graph's schema, so the exact tools may vary. A typical set of generated tools falls into three categories:

[cols="1,3"]
|===
| Tool Type | Generated Tools

| **Cypher Templates**
| Aircraft Systems and Components, Aircraft Maintenance Events, Aircraft Flights and Delays

| **Similarity Search**
| Search Maintenance Documentation

| **Text2Cypher**
| Natural Language to Cypher Tool
|===

== Step 5: Test the Agent

Test your agent with the sample questions below. After each test, observe which tool the agent selected, the context retrieved from the knowledge graph, and how the agent synthesized the response.

NOTE: The Cypher Template tools use internal identifiers (e.g., aircraft ID `AC1001` rather than tail number `N95040A`). You can ask the agent to look up an aircraft by tail number first, then use the returned aircraft ID in follow-up questions.

=== Cypher Template Questions

Try: **"What systems does aircraft AC1001 have?"**

The agent selects the Aircraft Systems and Components tool and traverses the HAS_SYSTEM relationships from the Aircraft node to return the engines, avionics suite, and hydraulic system.

Other questions to try:

* "Show maintenance events for aircraft AC1001" -- Uses Aircraft Maintenance Events to find faults, severity levels, and corrective actions.
* "What flights has aircraft AC1001 operated?" -- Uses Aircraft Flights and Delays to return routes, schedules, and delay causes.

=== Semantic Search Questions

Try: **"What do the maintenance procedures say about engine vibration troubleshooting?"**

The agent uses the Search Maintenance Documentation tool to find semantically relevant passages from the maintenance manual, then synthesizes the diagnostic steps and corrective actions.

Other questions to try:

* "How do I troubleshoot EGT overheat?"
* "What are the hydraulic system pressure limits?"
* "What causes bearing wear in turbine engines?"

=== Text2Cypher Questions

CAUTION: Text2Cypher can silently return wrong answers. Try this experiment: first ask _"Which aircraft had the highest number of maintenance events?"_ The agent correctly identifies AC1014 with 26 events. Now ask _"How many critical maintenance events does aircraft AC1014 have?"_ You may get a confident answer of zero -- even though AC1014 actually has 7 CRITICAL events. Run the same question again and you might get the correct answer. Text2Cypher generates a different Cypher query each time, and subtle bugs (e.g., filtering on `"critical"` instead of `"CRITICAL"`) can produce incorrect results. **Cypher Templates are deterministic and always return correct results.**

Try: **"Which aircraft have the most maintenance events?"**

The agent translates this natural language question into a Cypher aggregation query that counts MaintenanceEvent nodes per Aircraft and returns the top results.

image::lab5-agent-chat.png[Agent Query]

Other questions to try:

* "What are the most common fault types across all maintenance events?" -- Generates a query grouping and counting maintenance events by fault type.
* "Which airports have the most departing flights?" -- Aggregates flights by departure airport to show the busiest hubs.

== Step 6: (Optional) Deploy to API

1. Click **Deploy** in the Aura Agent console
2. Copy the authenticated API endpoint
3. Use the endpoint in your applications

== Step 7: (Optional) Connect as an MCP Server

You can connect your Aura Agent to MCP-compatible clients like Claude Code, Claude Desktop, VS Code, or Cursor.

1. Enable **External access** and toggle **Enable MCP server** on your agent

image::lab5-mcp-setup.png[Enable External and MCP Server]

Then:

1. Copy the MCP server endpoint URL from the agent menu
+
image::lab5-mcp-endpoint.png[Copy MCP Server Endpoint]

2. Get your API credentials from Account Settings > API Keys
3. Configure your client using the connection details

== Step 8: (Optional) Add a Flight Lookup by Number Tool

The AI-generated Cypher Templates take an `aircraft_id` as input, so looking up a specific flight by its flight number (e.g., "EX370") relies on the Text2Cypher tool. If you want a deterministic Cypher Template for flight number lookup, you can add one manually:

1. In the Aura Console, open your agent and click **Add tool** > **Cypher Template**
2. Name it **Get Flight Details by Number**
3. Use this Cypher query:
+
[source,cypher]
----
MATCH (f:Flight {flight_number: $flight_number})
OPTIONAL MATCH (f)<-[:OPERATES_FLIGHT]-(a:Aircraft)
OPTIONAL MATCH (f)-[:DEPARTS_FROM]->(origin:Airport)
OPTIONAL MATCH (f)-[:ARRIVES_AT]->(destination:Airport)
OPTIONAL MATCH (f)-[:HAS_DELAY]->(d:Delay)
RETURN f.flight_number AS flightNumber, a.aircraft_id AS aircraftId,
       f.operator AS operator, origin.iata AS origin, destination.iata AS destination,
       f.scheduled_departure AS scheduledDeparture, f.scheduled_arrival AS scheduledArrival,
       d.cause AS delayCause, d.minutes AS delayMinutes
----
4. Set the parameter `flight_number` with a description like "The flight number (e.g., EX370)"

With this tool added, the agent can handle questions like "Show details for flight EX370" deterministically.

NOTE: The exact tools generated by "Create with AI" depend on your graph's schema and the AI's interpretation of your prompt. If your agent generated different tools, you can always add, remove, or edit tools manually.

== Summary

The "Create with AI" workflow generated an agent with three retrieval patterns:

[cols="1,2,3"]
|===
| Tool Type | Purpose | Best For

| **Cypher Templates**
| Controlled, precise graph traversals
| Aircraft systems, component hierarchy, maintenance history

| **Similarity Search**
| Semantic retrieval over maintenance manuals
| Finding troubleshooting procedures by meaning

| **Text2Cypher**
| Flexible natural language to Cypher
| Ad-hoc questions, aggregations, cross-entity queries
|===

Congratulations! You have completed the workshop. You can optionally connect your agent as an MCP server for use with Claude Code, VS Code, or other MCP-compatible clients. See the MCP Server Setup guide (`mcp_setup.md`) in the lab materials.
