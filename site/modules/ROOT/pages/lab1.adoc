= Lab 1: Neo4j Aura Setup

This is Lab 1 of 4. The graph database you provision here is the foundation for everything that follows: the ETL pipelines in Lab 2 load data into it, Lab 3 runs semantic search against its maintenance manuals, and Lab 4 connects it to a multi-agent system. Getting Aura running and understanding what the graph contains is the starting point.

TIP: Ready to start? Jump to the xref:lab1-instructions.adoc[Lab 1 Instructions] for step-by-step setup.

.**At a Glance**

* **Graph databases** store data as nodes and relationships — connections are explicit and traversable, not hidden in foreign keys and joins
* **Cypher** is Neo4j's pattern-matching query language — queries read like the graph patterns they match
* **The Aircraft Digital Twin** graph has five entity types: Aircraft, Systems, Components, Flights, and Maintenance Events
* **Neo4j Aura** is the managed cloud graph database, with built-in vector search, Graph Data Science, and Aura Agents
* **Aura developer tools**: Query Workspace (Cypher editor), Explore (visual graph canvas), and Dashboards (low-code visualization)

== Graph Databases

A graph database models data as nodes and relationships. Nodes represent entities. Relationships represent the connections between them. Both carry properties, which are key-value pairs that describe their attributes.

The notation follows a visual convention. Parentheses denote nodes, brackets denote relationships:

----
(:Component {name, type})-[:HAS_EVENT {date, severity}]->(:MaintenanceEvent)
----

Each Component node carries properties like name and type. Each HAS_EVENT relationship carries details like date and severity. The relationship is directional, typed, and stored as a first-class element alongside the nodes it connects.

.How graphs compare to relational databases
[%collapsible]
====
Where relational databases represent connections implicitly through foreign keys and join tables, graphs make them explicit and traversable. Questions that require following chains of connections become single traversal patterns rather than nested JOINs or complex nested joins.
====

image::lab1-property-graph.png[Aircraft Digital Twin property graph showing Aircraft, System, and Maintenance nodes connected by typed relationships]

.Graph Insight — Maintenance Impact Chain
[%collapsible]
====
A single maintenance event connects to both the affected engine _and_ the aircraft through typed relationships (`AFFECTS_SYSTEM`, `AFFECTS_AIRCRAFT`). This triangle of connections means you can start from any node and traverse to the others: "Which aircraft have critical faults?" or "What maintenance events affect Engine #1?" — queries that would require multiple JOINs in a relational database are single traversal patterns in the graph.
====

Cypher is the query language for interacting with Neo4j. It uses a pattern-matching syntax that mirrors the visual structure of nodes and relationships, making graph queries readable as descriptions of the data they retrieve:

[source,cypher]
----
MATCH (a:Aircraft)-[:HAS_SYSTEM]->(s:System)-[:HAS_COMPONENT]->(c:Component)
WHERE a.tail_number = 'N95040A'
RETURN s.name, c.name
----

.How the query works
[%collapsible]
====
The query reads like the pattern it matches: start at an Aircraft node, follow HAS_SYSTEM to a System, then HAS_COMPONENT to a Component. Cypher handles multi-hop traversals and path-finding operations natively.
====

== The Aircraft Digital Twin Graph

The workshop builds a knowledge graph representing aircraft digital twins. Five core entity types and the relationships between them capture the structure of an aircraft fleet, its operations, and its maintenance history:

[cols="1,3"]
|===
| Entity Type | Examples

| **Aircraft**
| Boeing 737-800, Airbus A320/A321, Embraer E190

| **Systems**
| Engines, Avionics, Hydraulics

| **Components**
| Turbines, Compressors, Pumps

| **Flights**
| Flight operations with departure/arrival airports

| **Maintenance Events**
| Fault tracking with severity levels
|===

These entities connect through typed relationships that reflect real-world structure:

----
(Aircraft)-[:HAS_SYSTEM]->(System)-[:HAS_COMPONENT]->(Component)
(Aircraft)-[:OPERATES_FLIGHT]->(Flight)-[:DEPARTS_FROM]->(Airport)
(Component)-[:HAS_EVENT]->(MaintenanceEvent)
----

A question like "Which components caused flight delays for aircraft N95040A?" requires traversing from Aircraft through Flight to Delay, then back through MaintenanceEvent to Component. That multi-hop traversal is what the graph handles natively.

image::dual-database-architecture.png[Dual-database architecture showing Neo4j and Databricks working together for the Aircraft Digital Twin]

.Dual-database architecture
[%collapsible]
====
The Aircraft Digital Twin spans two platforms. Neo4j holds the topology: how aircraft, systems, components, flights, and maintenance events connect. Databricks holds the volume: high-frequency sensor telemetry, fleet-wide aggregations, and governed Delta tables. Aircraft, systems, and sensors exist in both databases as join points, enabling queries that combine graph traversal with analytical computation.
====

== Neo4j Aura

Neo4j Aura is a fully managed cloud graph database service. It provides the Neo4j graph engine without the operational overhead of running infrastructure: automatic backups, high availability, and deployment across AWS, GCP, and Azure. Each participant provisions their own Aura instance to hold the Aircraft Digital Twin graph. The data loaded in Lab 2 persists in this instance and is queried by notebooks in Labs 3 and 4.

.Vector search, Graph Data Science, and AI capabilities
[%collapsible]
====
Aura provides capabilities beyond the core graph engine that support AI retrieval pipelines directly. Built-in vector indexes store embeddings alongside graph data, enabling semantic similarity search without a separate vector store. Cypher queries can combine vector search with graph traversal in a single operation, retrieving documents by semantic similarity and then following relationships to pull in structural context. Graph Data Science algorithms like PageRank and community detection run directly within Aura for analytical workloads. These capabilities come together in Lab 3 (semantic search over maintenance manuals).
====

.Aura Agents — no-code agent platform
[%collapsible]
====
Aura Agents is Neo4j's no-code agent platform for building conversational interfaces grounded in graph data. Users describe a use case, and the platform auto-generates an agent with tools tailored to the graph's schema: Cypher query templates for deterministic traversals, vector similarity search over embedded content, and text-to-Cypher generation for ad-hoc exploration. Agents deploy as REST API endpoints or MCP server endpoints, making them callable by external orchestrators like the Databricks supervisor agent built in xref:lab4.adoc[Lab 4].
====

== Aura Developer Tools

The Aura console provides three tools you will use throughout the workshop.

**Query Workspace** is a developer environment for writing and executing Cypher queries with syntax highlighting, auto-completion, and saved query collections. You will use this to verify data loads and explore results.

**Explore** (powered by Neo4j Bloom) provides visual graph exploration through an interactive canvas. Search for nodes by label or property, expand their relationships, and discover patterns visually. Natural language search translates questions into graph queries without writing Cypher.

**Dashboards** offer low-code data visualization including bar charts, geographic maps, and 3D graph visualizations for presenting graph data to non-technical stakeholders.

== Next Steps

Continue to xref:lab1-instructions.adoc[Lab 1 Instructions] to set up your Neo4j Aura database and practice Cypher basics. See xref:lab1-sample-queries.adoc[Sample Queries] for hands-on Cypher examples to run against the graph. When ready, proceed to xref:lab2.adoc[Lab 2: Databricks ETL to Neo4j].
