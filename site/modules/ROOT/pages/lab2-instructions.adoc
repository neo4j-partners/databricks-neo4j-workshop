= Lab 2 Instructions: Databricks ETL to Neo4j

Follow these steps to load aircraft data from Databricks into Neo4j using the Spark Connector.

TIP: For concepts and background, see xref:lab2.adoc[Lab 2: Concepts and Reference].

**Duration:** ~45 minutes

== Prerequisites

* Neo4j Aura credentials from Lab 1 (URI, username, password)
* Vocareum lab environment access

== Notebooks

This lab has a single notebook. Run it before proceeding. It teaches how the Spark Connector works while loading the complete canonical dataset needed by all subsequent labs, including the Neo4j MCP agent in Lab 4 (Compound AI Agents).

[cols="2,4,1"]
|===
| Notebook | Description | Required For

| `01_aircraft_etl_to_neo4j.ipynb`
| Guided Spark Connector walkthrough that loads the complete canonical dataset: Aircraft, Systems, Components, Sensors, Airports, Flights, Delays, Maintenance Events, and Removals. Clears the database first (`CLEAR_DATABASE = True`)
| Labs 3, 4
|===

== Instructions

Use the Vocareum lab setup to complete the Databricks workspace configuration and run the ETL notebooks.

== What You Loaded

=== Full Aircraft Digital Twin Graph

----
(Aircraft) -[:HAS_SYSTEM]-> (System) -[:HAS_COMPONENT]-> (Component)
(Aircraft) -[:HAS_SYSTEM]-> (System) -[:HAS_SENSOR]-> (Sensor)
(Aircraft) -[:OPERATES_FLIGHT]-> (Flight) -[:DEPARTS_FROM / :ARRIVES_AT]-> (Airport)
(Flight) -[:HAS_DELAY]-> (Delay)
(Component) -[:HAS_EVENT]-> (MaintenanceEvent)
(Aircraft) -[:HAS_REMOVAL]-> (Removal) -[:REMOVED_COMPONENT]-> (Component)
----

== Troubleshooting

**"Connection refused" or timeout errors** -- Verify your Neo4j URI starts with `neo4j+s://`, check your instance is running, confirm credentials are correct.

**"Spark Connector not found"** -- Ensure you are using the workshop compute (not Serverless). The compute must be in Dedicated (Single User) access mode.

**"Path does not exist" for data files** -- Verify the DATA_PATH matches your workshop configuration. Ask your instructor for the correct Volume path.

**Duplicate nodes appearing** -- The notebook uses Overwrite mode. If needed, clear your database: `MATCH (n) DETACH DELETE n`

**Notebook cells failing** -- Run cells in order from top to bottom. Do not skip the configuration cells. Check the error message for specific issues.

== Help

* Ask your instructor for assistance
* https://neo4j.com/docs/spark/current/[Neo4j Spark Connector docs]
* https://neo4j.com/docs/cypher-manual/current/[Cypher Query Language reference]

== Next Steps

After completing this lab:

* Continue to xref:lab3.adoc[Lab 3: Semantic Search] to add GraphRAG capabilities over maintenance documentation
* Continue to xref:lab4.adoc[Lab 4: Compound AI Agents] to build a Supervisor Agent with Genie space and Neo4j MCP
* The data you loaded will be queried by AI agents in later labs
